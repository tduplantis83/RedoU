import { MeasurementConverterPipe } from './measurement-converter.pipe';

describe('MeasurementConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new MeasurementConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
